let pool = DatabaseConnectionPool::new("postgres://localhost/mydb");
let conn = pool.get_connection();

let pool = DatabaseConnectionPool::new("mysql://localhost/mydb");
let conn = pool.get_connection();

let pool = DatabaseConnectionPool::new("sqlite:///mydb.db");
let conn = pool.get_connection();

[dependencies]
diesel = { version = "1.4.4", features = ["postgres", "mysql", "sqlite"] }
